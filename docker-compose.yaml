version: '3.1'

services:

  # mongo:
  #   image: mongo
  #   restart: always
  #   volumes:
  #     - ./data:/data/db
#     environment:
#       MONGO_INITDB_ROOT_USERNAME: root
#       MONGO_INITDB_ROOT_PASSWORD: example

  frontend:
    build: frontend
    links:
      - "backend:api"
  
  backend:
    build: backend
    ports:
      - "3068:3068"
